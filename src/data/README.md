# Data files

This folder is reserved for the real Wind Waker randomizer data:

- `locations.txt` for all sector, sub-area, and separate-area locations.
- `items.txt` for item names.
- Additional structured data can be added later when the UI needs exact map/icon placement.

The current prototype has temporary sample data in `app.js` so the interface can be tested before the full lists are added.
